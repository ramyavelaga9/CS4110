Designed a VM translator that would take a VM code and translate it to corresponding assembly code
while detecting syntax errors. The input to VM translator would be .vm file and the output would be
.asm file.
